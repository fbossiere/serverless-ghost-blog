# ==============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ==============================================================================
#
# This file shows the format for local development with docker-compose.
#
# AUTOMATIC GENERATION (Recommended):
#   If you deploy with Terraform, this file is auto-generated as .env
#   Run: cd terraform && terraform apply
#   Then: cd .. && docker-compose up
#
# MANUAL SETUP (for manual deployments):
#   1. Copy this file: cp .env.example .env
#   2. Fill in the values from your Scaleway Console
#   3. Never commit .env to git (it's in .gitignore)
#
# ==============================================================================

# Scaleway Credentials (for S3)
SCW_ACCESS_KEY=your_access_key
SCW_SECRET_KEY=your_secret_key
SCW_REGION=fr-par

# Scaleway Object Storage
SCW_BUCKET_NAME=your-bucket-name

# Scaleway Managed Database
SCW_DB_HOST=your_db_ip
SCW_DB_PORT=your_db_port
SCW_DB_USER=ghost
SCW_DB_PASSWORD=your_db_password
SCW_DB_NAME=ghost
